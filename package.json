{
  "name": "cloudfire-apiv1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "db:migrate:remote": "wrangler d1 migrations apply cloudfire-db --remote",
    "db:migrate:staging": "wrangler d1 migrations apply cloudfire-db-staging --env staging --remote",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250430.0",
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^11.10.0",
    "eslint": "^10.3.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.59.2",
    "vitest": "^4.1.5",
    "wrangler": "^4.86.0"
  },
  "dependencies": {
    "chanfana": "^2.4.3",
    "drizzle-orm": "^0.43.1",
    "hono": "^4.7.10",
    "zod": "^4.0.0"
  }
}
